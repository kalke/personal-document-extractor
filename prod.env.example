# Copy to prod.env (gitignored) and fill values.
# source with: set -a; source prod.env; set +a

PORT='8080'
DATABASE_URL='postgresql://neondb_owner:PASSWORD@HOST-pooler/neondb?sslmode=require'
# Schema isolation on shared Neon (must match RuntimeParams search_path in the API).
DB_SEARCH_PATH='pde'
REDIS_ADDR='YOUR.upstash.io:6379'
REDIS_PASSWORD=''
REDIS_TLS='true'
REDIS_DB='0'
OIDC_ISSUER='https://auth.kalke.dev/realms/kalke'
OIDC_AUDIENCE='personal-document-extractor'
AUTH_INTROSPECT_URL='https://auth.kalke.dev/v1/introspect'
INTROSPECT_SECRET=''
ADMIN_EMAILS='henriquekalke@icloud.com'
CORS_ORIGINS='https://kalke.dev,https://www.kalke.dev,http://localhost:5173'
GROQ_API_KEY=''
LOG_LEVEL='info'
LOG_FORMAT='json'
RATE_LIMIT_PER_MINUTE='60'
